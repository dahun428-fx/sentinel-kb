# 모든 프록시 location이 공유하는 설정 (T-026).
proxy_http_version 1.1;

proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host  $host;

# 업스트림 keepalive 풀을 쓰려면 hop-by-hop `Connection`을 비워야 한다.
proxy_set_header Connection "";

# Authorization은 nginx가 기본으로 넘긴다. **여기서 지우거나 덮어쓰지 않는다** —
# MCP는 호출자 Bearer를 그대로 core-api로 전달하는 설계다(confused deputy 방지, T-014 D-5).
# 프록시가 헤더를 만지면 그 설계가 조용히 깨진다.

proxy_connect_timeout 5s;
