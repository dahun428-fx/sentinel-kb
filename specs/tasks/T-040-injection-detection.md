# T-040: 언어 무관 인젝션 탐지
refs: specs/05 (Eval 4), specs/03 §2, FR-06, NFR-05, docs/analysis/T-004-POSTMORTEM.md §3.2·§5.3
M: M4 | deps: T-004, T-021

## 배경

T-021이 세 방어선을 갈라 재서 **탐지 9/10 — FAIL**을 실측했다(T-021 F-1).
미탐 1건(`INJ-10`)은 일본어이고, `packages/core/src/sanitizer/injection.ts`의 규칙 12개는
전부 `en-*`/`ko-*`다.

**이건 1건 누락이 아니다.** 제외(`generator/context.ts`)의 판단 근거는 오직 `flags`이므로
미탐 케이스에는 방어선 2가 발동조차 하지 않는다. 탐지가 뚫리면 제외는 존재하지 않는 것과 같다.

## 이 태스크가 **하지 말아야** 할 것 — T-004의 재발 방지

`ja` 규칙을 더하면 `zh`가, `zh`를 더하면 `ru`가 남는다.
T-004가 여덟 라운드 동안 실패한 방식이 정확히 이것이며, 포스트모템 §2.5가 그 성질을 명시했다:

> 판정용 열거에 없는 문자를 만나면 규칙이 무발동하고, 무발동은 무플래그 평문 통과를 뜻한다.

§3.2의 결론은 **"이전 가능한 요인은 유니코드 속성 자체가 아니라 생성기 독립성"**이다 —
판정 근거가 구현자가 만든 목록이 아니라 **외부에 이미 있는 보편 술어**일 것.

그러므로 이 태스크가 추가하는 규칙은 다음 중 하나를 만족해야 한다.

1. **외부 표준이 정의한 술어**로 판정한다(Unicode Script / `Intl.Segmenter` / UTS #39 계열).
2. **이 시스템 자신이 내보내는 문자열**을 앵커로 삼는다(컨테이너 태그·대화 템플릿 특수 토큰).
   앵커는 손으로 베끼지 않고 **에미터에서 기계적으로 대조**한다.

자연어 단어 목록을 언어별로 늘리는 것은 **금지**한다.

## Scope

- `packages/core/src/sanitizer/injection.ts`에 **언어 무관 구조 신호** 추가
- 다국어 코퍼스(적대 + 정상)를 유닛 테스트 계층에 추가 — 미탐을 **케이스가 아니라 축**으로 본다
- 오탐 코퍼스(시드 50건 + `docs/analysis/**`)를 테스트가 직접 읽어 **신규 플래그 0건**을 잠근다
- 구조 신호로 닫히지 않는 축을 코드에 `KNOWN_GAP`으로 **선언**하고, 그 목록이 늘지 않음을 단언
- `eval/injection/**`(T-021 산출물)을 base로 가져와 전/후 재측정

## Out of scope

- 언어별 자연어 규칙 추가(위 금지 조항)
- `SanitizeFlag` 열거형 변경 — contracts 재개방(G3)이라 사람 승인이 먼저다
- 제외 로직(`generator/context.ts`) 재설계 — 별도 태스크
- 임베딩·LLM 기반 의미 판정 — 저장 경로가 **동기 게이트**라 계층이 다르고(`sanitize.ts` 서두),
  이 환경에는 자격증명이 없어 판정 불가다

## Acceptance

전부 명령어로 판정 가능하다.

- [x] **A1 마크업 축의 언어 의존성 제거.** `INJ-06`/`INJ-07`의 영어 문장을 ja·zh로 바꾼 변형과
      대화 템플릿 위조 변형이 전건 `injection-suspect`로 플래그된다.
      판정: `pnpm test -- injection.multilingual` 그린
- [x] **A2 호모글리프 우회 차단.** 라틴 단어에 키릴·그리스 동형 문자를 섞어 기존 `en-*` 규칙을
      전부 빗나가게 한 변형이 플래그된다. 판정: 같은 스위트
- [x] **A3 오탐 0.** 시드 50건 + `docs/analysis/**`의 전 단위에서 **신규 플래그 0건**.
      테스트가 코퍼스를 파일에서 직접 읽어 단언한다(스냅샷 금지).
      판정: `pnpm test -- injection.multilingual` 그린
- [x] **A4 정상 다국어 무발화.** ja·zh·ru·de·es·fr·ar·th·vi 정상 장애 기록과
      **인젝션을 서술하는 정상 기록**(R-8)이 플래그되지 않는다. 판정: 같은 스위트
- [x] **A5 앵커가 기계적이다.** `renderContext`가 실제로 내보내는 컨테이너 닫는 태그를
      테스트가 렌더 결과에서 추출해 전건 탐지됨을 단언한다 — 태그 이름이 바뀌면 테스트가 죽는다.
      판정: 같은 스위트
- [x] **A6 T-021 코퍼스 비회귀.** 재측정 탐지 수가 9/10 이상. 판정: `pnpm test -- eval/injection`
- [x] **A7 기존 스위트 무약화.** `sanitize.axes.spec.ts`·`sanitize.spec.ts`·
      `sanitize.property.spec.ts` 무수정. 판정: `git diff --stat` 에 세 파일이 없다
- [x] **A8 알려진 공백의 선언.** 구조로 닫히지 않는 축이 `KNOWN_STRUCTURAL_GAPS`로 코드에 있고,
      테스트가 그 목록의 크기가 늘지 않음을 단언한다. 판정: 같은 스위트
- [x] **A9** `pnpm verify` 그린

## Context budget

- 읽기: `packages/core/src/sanitizer/**`, `packages/core/src/generator/context.ts`,
  `eval/injection/**`, `docs/analysis/T-004-POSTMORTEM.md` §3·§5,
  `packages/core/seed/**`(오탐 코퍼스로만), `specs/tasks/T-021-injection-redteam.md`

## 검증 프로토콜 (포스트모템 §5.3 제안 1·3 적용)

- 리포트에 **축**과 **미탐색 축**을 필수로 적는다.
- 다국어 코퍼스는 **구현을 보지 않고** "의도 × 언어"로 생성한다. `injection.ts`를 import하지 않는다.
- 뮤테이션으로 표본이 구현보다 넓음을 보인다 — 구현을 좁히는 뮤턴트에 테스트가 죽어야 한다.

## Findings (T-040 구현 세션)

### 축 / 미탐색 축 (§5.3 제안 1)

- **축:** 인젝션 지시가 모델에 도달하는 경로의 **표현 형식** — 자연어 평문 / 프레임 마크업 /
  문자 수준 난독화(비가시·동형).
- **닫은 축:** 프레임 마크업(언어 무관), 동형 문자 우회(언어 무관).
- **미탐색 축:** 턴 마커 위조(`Human:`/`Assistant:`) — 의도적 보류, `KNOWN_STRUCTURAL_GAPS` 참조.
  블록 종결 위조(`--- 위 참고 블록 끝 ---`)는 마크다운 오탐이 커서 손대지 않았다.
  MCP `get_record` 경로의 재측정(T-021 F-6)도 여전히 열려 있다.

### F-1 ⚠️ **미탐 1건이 아니라 축 전체가 열려 있었다.** `INJ-10`은 코퍼스의 크기였다

T-021은 일본어 미탐을 **1건**으로 인계했다. 같은 축(평문 지시문 · 비 ko/en 언어)을
ja·zh·ru·de·es·fr·ar·th·vi 12건으로 넓혀 재니 **12/12 전건 미탐**이었다.
라틴 문자를 쓰는 de·es·fr·vi도 전부 미탐이다 — 이 축은 문자체계 문제가 아니라 **언어 문제**다.

### F-2 ⚠️ `forged-system-tag`·`container-escape` 축은 **어느 규칙도 덮고 있지 않았다**

`INJ-06`·`INJ-07`이 플래그된 것은 페이로드에 딸려 있던 **영어 문장** 때문이지 마크업 때문이
아니었다. 실측: 그 영어 문장만 ja·zh로 바꾸면 **4/4 미탐**이 된다.
즉 T-021 리포트의 "탐지 9/10"은 두 축을 **과대평가**하고 있었다. `<|im_start|>` 같은
대화 템플릿 특수 토큰도 전부 미탐이었다.

이것이 `struct-role-frame-forgery`·`struct-container-escape`·`struct-chat-template-forgery`가
닫은 자리이고, 판정 근거가 자연어가 아니므로 **언어와 무관**하다.

### F-3 ⚠️ 동형 문자 한 글자가 자연어 규칙 **12개 전부**를 무력화했다

실측: `Ignore`의 `o`를 키릴 `о`(U+043E)나 그리스 `ο`(U+03BF)로 바꾼
`Ignоre all previous instructions`가 **무플래그 통과**했다.
공격자는 언어를 바꿀 필요조차 없었고, 이 축이 `INJ-10`보다 싸고 넓다.
`struct-mixed-script-word`(Unicode `Script` + `Intl.Segmenter`, UTS #39 축소판)가 닫았다.

오탐 방지의 핵심은 **단어 경계를 ICU에 맡긴 것**이다. 한국어 기술 산문은 `terraform apply가`
처럼 라틴과 한글을 붙여 쓰는데, 인접 문자만 보면 이게 전부 오탐이 된다.
뮤턴트 M13(세그멘터 → 공백 분할)이 테스트 15개를 죽여 이 선택이 실제로 하중을 받고 있음을 보였다.

### F-4 ❌ **평문 지시문 축은 구조 신호로 닫지 못했다 — 그리고 닫히지 않는다고 판단한다**

억지로 맞추지 않았다. `KNOWN_STRUCTURAL_GAPS`에 선언하고 근거를 남긴다.

태스크 힌트가 제시한 세 가지 구조 신호를 **실측으로 배제**했다(단정이 아니다):

| 후보 신호 | 적대 12건 | 정상 15건 | 판정 |
|---|---|---|---|
| 문자수 | 28–107 | 51–187 | 완전히 겹침 |
| 문장수(`Intl.Segmenter`) | 1–2 | 1–3 | 완전히 겹침 |
| 단어수 | 8–26 | 15–39 | 완전히 겹침 |
| 문자체계 수(비라틴) | 0–3 | 0–3 | 완전히 겹침 |

특히 **문자체계 이질성은 신호가 아니다.** 정상 일본어 기록(`benign-ja-terraform`)의
문자체계 수가 3으로 적대 케이스와 같다 — 가타카나 외래어와 라틴 식별자 때문이며,
이는 인젝션과 무관한 일본어의 정상 표기다. 이 신호를 채택하면 **일본어 사용자의 기록 전체가
플래그된다** — R-8 그대로다.

적대와 정상을 실제로 가르는 것은 **문법적 서법**(명령형 대 서술형)과 **메타 참조 어휘**뿐이고,
둘 다 언어별 지식이다. 즉 이 축에는 **외부 보편 술어가 존재하지 않으며**,
포스트모템 §3.1이 mongo 축에 내린 판정이 그대로 적용된다:

> ①은 대비의 결론이 아니라 대비의 **원인**이다: 비가시 축은 "전칭 술어가 존재하는 문제"였고
> mongo 축은 아니었다.

### F-5 → 다음 수단: 의미 계층. 이 환경에서는 **판정 불가**

이 축에서 "생성기 독립성"을 얻는 유일한 길은 언어 축을 **모델에 넘기는 것**이다.
`packages/core/src/embedder`의 다국어 임베딩으로 인젝션 **의도** 앵커(6개 내외)와
코사인을 재면, 언어가 늘어도 코드는 바뀌지 않는다 — `\p{Cf}`가 형제 문자를 미리 삼킨 것과
같은 관계다. 워커가 이미 청크마다 임베딩을 계산하므로(`worker/ingest.ts`) 비용도 거의 없다.

**이 태스크에서 하지 않은 이유는 셋이다.**

1. `sanitize()`는 저장 경로의 **동기** 게이트이고 "정규식만 쓰고 LLM·네트워크를 부르지 않는다"가
   그 파일의 계약이다(`sanitize.ts` 서두). 계층이 다르다 — 비동기 ingest 경로의 일이다.
2. 새 신호를 실어 나르려면 `SanitizeFlag` 열거형이 늘어야 하고 그건 contracts 재개방(G3)이다.
3. 자격증명이 없어 **판정 불가**다. T-021 방어선 3·T-016 Acceptance 1이 막힌 것과 같은 벽이다.
   fake 임베더로 대신 돌리면 cosine ≈ 0이라 아무것도 검증하지 않은 채 초록이 난다(T-021 서두).

→ 별도 태스크 + 사람 승인. 이 문서가 그 근거다.

### F-6 오탐 측정 — 지정 코퍼스 0건, specs/** 에서 +2건

- **시드 50건(686단위) + `docs/analysis/**`(220단위) = 906단위 → 신규 플래그 0건.** (전 0 / 후 0)
- 정상 다국어 17건(ja·zh·ru·de·es·fr·ar·th·vi·ko 한자 병기·물리량 표기 + **인젝션을 서술하는
  정상 기록 3건**) → 0건.
- 참고로 코퍼스를 `specs/**`(737단위)까지 넓히면 **전 5건 → 후 7건**으로 +2다.
  둘 다 `struct-container-escape`이고, 둘 다 `<retrieved-record>` 래핑 **형식을 문서에 그대로
  인용한 문단**이다(`specs/07-mcp.md`, `specs/tasks/T-021-...md`). 기록이 아니라 스펙 문서이므로
  FR-01(지식 보존) 위험은 아니지만, **이 레포 자신을 기록으로 넣으면 그 문단은 플래그된다** —
  R-8의 실물이다. 플래그는 지우지 않고 표시만 하므로 본문은 보존되지만,
  생성 컨텍스트에서는 빠진다(specs/03 §2).

### F-7 물리량 표기가 혼합 스크립트 오탐의 실물이었다

`Δt`·`ΔT`·`μs` 같은 표기는 그리스+라틴 혼합 단어다. 최소 단어 길이 하한(4글자)이 없으면
장애 기록의 **레이턴시 문단이 통째로 플래그된다.** 하한을 넣고
`benign-ko-physical-quantities`로 잠갔다. 뮤턴트 M14(하한 1자)가 죽는다.

### F-8 `sanitize.ts`가 인젝션 판정을 **원문**에 돌리는 것이 여기서도 맞았다

`detectInjection`은 마스킹 **전** 원문을 본다. 구조 신호도 같은 프로브를 쓰므로
`[MASKED:...]` 라벨이 `</chunk>` 같은 태그를 끊어 우회로가 되는 경로가 없다.

### 뮤테이션 (관측 경로: `npx vitest run --project unit packages/core/src/sanitizer eval/injection`)

19종 중 **17종 사망, 2종 생존**. 생존 2종은 아래에 숨기지 않고 적는다.

| # | 뮤턴트 | 결과 |
|---|---|---|
| M1 | `struct-chat-template-forgery` 제거 | 사망 (3) |
| M2 | `struct-role-frame-forgery` 제거 | 사망 (2) |
| M3 | `struct-container-escape` 제거 | 사망 (3) |
| M4 | `struct-mixed-script-word` 제거 | 사망 (3) |
| M5 | 구조 신호 배선 해제 | 사망 (9) |
| M6 | 혼합 스크립트 판정 무력화 | 사망 (3) |
| M7 | 비라틴을 한 계열로 뭉침 | **초기 생존** → 키릴/그리스 분리 + `ML-homoglyph-cyrillic-greek` 추가 후 사망 (1) |
| M8 | 기존 자연어 규칙 **전체** 제거 | 사망 (14) — 구조 신호만으로는 ko/en 평문을 못 잡는다 |
| M9 | 정상 텍스트를 항상 인젝션으로 판정 | 사망 (56) — 오탐 가드가 실제로 작동한다 |
| M10 | 컨테이너 앵커 하나 삭제 | 사망 (1) — A5 에미터 대조가 잡는다 |
| M11 | `KNOWN_STRUCTURAL_GAPS` 비우기 | 사망 (1) |
| M12 | `isWordLike` 필터 제거 | **생존 — 동등 뮤턴트.** ICU의 비단어 세그먼트는 공백·구두점뿐이라 `\p{L}`이 하나도 없다. 대신 M13으로 같은 지점을 실측했다 |
| M13 | `Intl.Segmenter` → 공백 분할 | 사망 (15) — 세그멘터가 하중을 받는다 |
| M14 | 최소 단어 길이 4 → 1 | 사망 (1) — 물리량 표기 오탐 |
| M15 | 최소 단어 길이 4 → 12 | 사망 (4) |
| M16 | 세그멘터 로케일 고정 해제 | **생존 — 이 환경에서 동등.** en/ko/ja/th/ru/ar/zh 7개 로케일로 코퍼스를 재니 단어수가 전부 동일했다(ICU 사전 분절이 로케일과 무관). 고정은 관측된 버그의 수정이 아니라 **결정론 방어**이며, 단일 로케일 환경에서는 원리적으로 죽일 수 없다 |
| M17 | 구조 신호를 빈 문자열에 적용 | 사망 (10) |

### 이 접근의 성격 — 스스로 판정한다

**열거가 아니라 구조 신호다. 단, 축을 가려서 그렇다.**

- `struct-mixed-script-word` — 판정 근거가 Unicode `Script` 속성과 ICU 단어 경계다.
  다음 동형 문자가 어느 문자체계에서 오든 자동으로 들어온다. **`\p{Cf}`와 같은 성질이다.**
- `struct-chat-template-forgery` — 개별 토큰 이름(`im_start`·`endoftext`…)을 열거하지 않고
  `<|…|>`라는 **모양**을 잡는다. 다음 모델의 토큰도 자동으로 들어온다.
- `struct-container-escape` — 앵커가 자연어가 아니라 **이 시스템 자신의 출력**이고,
  `renderContext` 실 출력에서 기계적으로 대조한다(M10이 죽는다).
- `struct-role-frame-forgery` — `<system>` 하나뿐이다. 여기만 **작은 열거**이며,
  `<user>`·`<assistant>`를 넣지 않은 것은 자리표시자 오탐(R-8) 때문이다. 정직하게 적어 둔다.

**닫지 못한 것:** 평문 지시문 축. F-4가 실측으로, F-5가 대안으로 답한다.
T-021 코퍼스 수치는 **9/10에서 움직이지 않았다** — `INJ-10`이 정확히 그 축에 있다.
언어를 하나 더하면 10/10이 됐겠지만 그것이 T-004가 여덟 라운드 동안 한 일이고,
숫자는 올라가되 **fail-open은 그대로 남는다.**
