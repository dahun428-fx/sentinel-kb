# 의도적 위반: 22 가 넓은 포트 범위 안에 숨어 있다. `grep 22` 로는 안 잡히는 형태.
resource "aws_vpc_security_group_ingress_rule" "wide" {
  security_group_id = aws_security_group.app.id
  cidr_ipv4         = "10.0.0.0/8"
  ip_protocol       = "tcp"
  from_port         = 20
  to_port           = 443
}
